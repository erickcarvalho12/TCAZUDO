@layout('layout/app')

@section('content')
  @if(flashMessages.has('errors'))
    <div class="alert alert-danger">{{ flashMessages.get('errors') }}</div>
  @endif
  <div class="row d-flex justify-content-center">
    <div class="col-4">s
      <main class="form-signin">
        <form action="{{ route('rifas.list') }}" method="post">
          <h1 class="h3 mb-3 fw-normal">Lista De Rifas</h1>

         
         
        </form>
      </main>
    </div>
  </div>
@endsection