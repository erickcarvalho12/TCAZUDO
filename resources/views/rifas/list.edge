@layout('layout/app')
@section('content')

  <ul class="list-group">
    @each(rifa in rifas)
      <li class="list-group-item list-group-item-light d-flex justify-content-between">
      <div class="title">
      <a class="negrito">Nome: </a> {{ rifa.titulo }}  
    </div>  
      <div class="d-flex justify-content-between">
        <a href="{{ route('rifas.show', { rifa_id: rifa.id }) }} " class ="btn btn-outline-primary btn-space"> VISUALIZAR</a>
       <button href="{{ route('rifas.show', { rifa_id: rifa.id }) }} " class ="btn btn-outline-primary btn-space"> COMPRAR </button>
 
    </div>
      </li>
    @endeach
  </ul>
@endsection